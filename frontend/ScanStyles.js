import { StyleSheet } from 'react-native';

const ScanStyles = StyleSheet.create({
    absoluteFillObject: {
        position: 'absolute',

        width: '100%',
        height: '100%',
    },
});

export default ScanStyles;